<h1>Sticker Property</h1>

<!-- layout --><table class="layout" cellspacing="1" border="0">
<!-- layout --><tr><td class="layout">
<table cellspacing="1" border="0">
<tr><td><%=link_to 'upload new', :action=>:new %></td></tr>
<tr><td><%=link_to 'index', :action=>:index, :id=>@item.id %></td></tr>
<tr><td><%=link_to 'edit', :action=>:edit, :id=>@item.id %></td></tr>
<tr><td><%=link_to 'update shape', :action=>:update_shape, :id=>@item.id %></td></tr>
</table>
<!-- layout --></td><td class="layout">

<table cellspacing="1" border="0">
<tr><th>title</th> <td><%= @item.title %></td></tr>
<tr><th>link</th>  <td><%= url_for :controller => 'caststudio', :action => 'rpc', :only_path => false %></td></tr>
<tr><th>description</th> <td> <%= @item.desc %></td></tr>
<tr><th>pubDate</th> <td> <%= @item.pubdate %></td></tr>
<tr><th>author</th>  <td><%= @item.author %></td></tr>
<tr><th>category</th><td> <%= @item.category %></td></tr>
<tr><th>enclosure.url</th><td>
<a target="_blank" href="<%= url_for :controller => 'audio', :action => @item.filename_without_ext, :format => 'wav', :only_path => false %>"><%= url_for :controller => 'audio', :action => @item.filename_without_ext, :format => 'wav', :only_path => false %></a>
</td>
<tr><th>enclosure.info</th><td>
length="0" 
type="<%= @item.filetype %>"
</td>
</tr> 
</table>

<!-- layout --></td></tr></table>
