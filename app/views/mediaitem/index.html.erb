<%= link_to 'new', :action => 'new'%>
<pre>
Title: <%= @title %>
Link: <%= url_for :controller => 'caststudio', :action => 'rpc', :only_path => false %>
Description: <%= @description %>
Language: <%= @language %>
PubDate: <%= @pubdate %>
Category: <%= @ch_category %>

<% @items.each do |i| -%>
item
title <%= i.title %>
link <%= url_for :controller => 'caststudio', :action => 'rpc', :only_path => false %>
description <%= i.desc %>
pubDate <%= i.pubdate %>
author <%= i.author %>
category <%= i.category %>
enclosure
  url="<%= url_for :controller => 'audio', :action => i.filename_without_ext, :format => 'wav', :only_path => false %>"
  length="0" 
  type="<%= i.filetype %>"

<% end -%>

