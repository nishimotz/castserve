<ul>
<li> title <%= @item.title %>
<li> link <%= url_for :controller => 'caststudio', :action => 'rpc', :only_path => false %>
<li> description <%= @item.desc %>
<li> pubDate <%= @item.pubdate %>
<li> author <%= @item.author %>
<li> category <%= @item.category %>
<li> enclosure
  url="<%= url_for :controller => 'audio', :action => @item.filename_without_ext, :format => 'wav', :only_path => false %>"
  length="0" 
  type="<%= @item.filetype %>"
</ul>
